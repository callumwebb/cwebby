box: debian
build:
  steps:
    - arjen/hugo-build:
      version: "0.17"
      theme: hugo-creative-portfolio-theme
deploy:
  steps:
    - s3sync:
      key-id: $KEY
      key-secret: $SECRET
      bucket-url: $BUCKET
      source-dir: $SOURCE
